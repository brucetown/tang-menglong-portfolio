# 唐梦龙｜AI 影像创意导演作品集

移动端优先的个人作品集网站，使用 Vite + React 构建。

## 本地运行

```bash
npm install
npm run dev
```

## 发布

推送到 `main` 后，GitHub Actions 会自动构建并部署到 GitHub Pages。

该仓库只包含站点源代码、`public/assets` 内已经优化的媒体文件及部署配置；不提交 `node_modules`、`dist`、原始素材或其他项目文件。
